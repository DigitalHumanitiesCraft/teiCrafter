# Handoff 2026-07-10: Gesamtrefactoring, Stand nach Phasen G/W/H und T-Teil

Action-layer note, no Promptotyping frontmatter. Durable decisions live in
knowledge/journal.md and specification.md Key Decisions; this file carries the
volatile re-entry state for the session that continues the refactoring.

## Plan and provenance

The approved master plan is
`C:\Users\Chrisi\.claude\plans\alles-wissen-muss-dann-temporal-shamir.md`
(phases G/W/H/T/P/F/S plus W9, acceptance criteria, operator gates). It was
built from four audits, two recon passes, three web-research briefs and two
code-verified designs; the design detail for the open phases (P: save.js and
recovery.js APIs, proof names, wordings; W9 scope; S remainder) is inside that
plan file and in this session's transcript.

## State (verified this session)

- Branch main, tip `e0405f1`, working tree clean, 3 commits ahead of
  origin/main at the time of writing (push happens right after this note).
- Gate: `node test/run_all.mjs` green, 48 passed 0 skipped 0 failed
  (new location; the old test/tools/run_all.mjs is gone).
- Types: `tsc --noEmit -p jsconfig.json` clean over 15 included modules;
  `types_check.mjs` is hard-fail now.
- Done and pushed earlier today: Phase G (guardrails commit `7b8455e`),
  Phase W (knowledge 13 to 11 docs, six commits `5ac0295..42aa85d`),
  Phase H (hygiene `5640e70`, gitattributes `8243c6d`).
- Done, committed, unpushed until now: Phase T commits `65aad52` (proofs and
  generators split, one-glob runner at test/run_all.mjs, both worked examples
  in the gate) and `52870eb` (shared test/proofs/_assert.mjs, inline-GND
  wiring proof folded into inline_gnd_check.mjs, harness runValidate dedup);
  scaffolding part 1 `e0405f1` (LICENSE MIT, CITATION.cff, CODE_OF_CONDUCT,
  issue templates).

## Open threads, in order

1. Phase T remainder (the interrupted agent's commit 3): CI-enforced L2 step
   (py-gate fetches a pinned TEI All RNG to test/schemas/ and runs validate.py
   with the RNG so schema validation really executes; local stays
   SKIP-with-warning), rewrite test/README.md to the new layout, update
   knowledge/testing.md gate command and folded-proof row. Also verify on the
   next CI run that the three bare-checkout SKIP guards landed
   (editor_roundtrip, hersch_loadability, port_parity failed on run
   29117527911; py-gate was green).
2. W9, operator-approved: specification.md deep pass. Losslessness model
   (byte vs string, encoding boundary, scope limit for "arbitrary TEI"),
   fix I.5 status to partially built, scope "nothing is written" to source
   files, resolve the document-panel vs index-fallback contradiction, compact
   Key Decisions into D-IDs with a fixed shape, clean Open Questions (static
   typing is decided: JSDoc plus tsc gate, this session), record today's
   decisions (MIT, CI deviation, handoff convention) as Key Decisions, trim
   proof-path mentions toward testing.md.
3. Phase P (product readiness): save.js createSaver with write/close split and
   abort on failed write; recovery.js on IndexedDB Blobs with two-slot
   ping-pong covering handle-backed documents; load honesty (well-formedness
   warning, zero-folio hint); manifest JSON Schema at docs/schema/ plus parity
   proof with fixtures under test/fixtures-synthetic/manifests/; error-surface
   rule; about.html LLM-claim fix. Full design in the plan file.
4. Phase F (frontend decomposition): editor-app.js and annotation-ui.js
   splits, dead exports (re-verify before deleting), escaper unification,
   two A11y fixes.
5. Phase S remainder: README badge and License section plus about.html to MIT
   (deliberately untouched so far), CONTRIBUTING.md (gate command is now
   node test/run_all.mjs), CHANGELOG.md, then the operator-gated release
   (tag v0.1.0, Zenodo, concept DOI into CITATION.cff).
6. Operator sight-check requested: the five ORCIDs in CITATION.cff came from
   web verification against zfdg.de/2025_008; glance before the release.

## The one next step

Finish the T remainder (thread 1): it is small, fully specified above, and
unblocks a green CI, which every later phase leans on.

## Shared and held files

- Nothing uncommitted, nothing stashed, no foreign changes in the tree.
- Local-only material unchanged and untouched: test/fixtures/real/*,
  docs/data/editor/{wb-codex,zbz-100,zbz-1000,depcha-wheaton} payloads,
  test/schemas/tei_all.rng, output/.
- Sibling repos (szd-htr, zbz-ocr-tei) untouched; no orders went out.
